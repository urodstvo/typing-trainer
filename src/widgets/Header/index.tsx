import { Logo } from '@/components/Logo/';
import { Timer } from '@/components/Timer';

export const Header = () => {
    return (
        <header>
            <Logo />
            <Timer />
        </header>
    );
};
